var abp = abp || {};

abp.modals.businessEdit = function () {
    var initModal = function (publicApi, args) {
        var l = abp.localization.getResource("Fhir");
        
        
        
        
        
    };

    return {
        initModal: initModal
    };
};
